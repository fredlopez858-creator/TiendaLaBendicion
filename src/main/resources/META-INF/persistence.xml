<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2" xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <persistence-unit name="tiendaPU" transaction-type="JTA">
        <!-- Este es el nombre JNDI que configuraremos en el servidor -->
        <jta-data-source>java:/TiendaDS</jta-data-source>
        <properties>
            <!-- Dialecto específico para SQL Server -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.SQLServer2012Dialect"/>
            <!-- Esto ayuda a Hibernate a actualizar la BD si haces cambios en la entidad. Usa "validate" en producción -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- Muestra en la consola las consultas SQL que se ejecutan. Muy útil para depurar -->
            <property name="hibernate.show_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>